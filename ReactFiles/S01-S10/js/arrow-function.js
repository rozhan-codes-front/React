// regular function
  
    function sum (number){
        return number+2;
    }

    sum(4);
    //6

// arrow function
// without this
    const sum = (number) => number+2;

    const sum = (number) => {
        return number+2
    }

    sum(4);
    // 6
